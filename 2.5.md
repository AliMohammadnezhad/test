# قوانین کلیدی کسب‌وکار (Business Rules) سامانه آموزش عمومی

## مقدمه
این مستند مجموعه قوانین و محدودیت‌های کسب‌وکار را برای سامانه آموزش عمومی توصیف می‌کند. این قوانین بر اساس نیازهای بیزینسی تعریف شده‌اند و باید در تمام لایه‌های سیستم (Business Logic) اعمال شوند تا رفتار سیستم یکپارچه و منطبق با واقعیت کسب‌وکار باشد.

قوانین به دسته‌های اصلی تقسیم شده‌اند و شامل شرایط، محدودیت‌ها و اقدامات لازم در هر حالت هستند.

## ۱. قوانین ثبت‌نام و ثبت‌نام در دوره (Enrollment)
1. هر کاربر (Student) فقط در صورتی می‌تواند در یک دوره (Course) ثبت‌نام کند که **ظرفیت دوره هنوز تکمیل نشده باشد**.
2. ثبت‌نام در دوره نیازمند **پرداخت موفق** هزینه دوره است (مگر دوره‌های رایگان).
3. کاربر نمی‌تواند همزمان در بیش از یک کلاس (instance) از همان دوره ثبت‌نام کند.
4. ثبت‌نام فقط تا **تاریخ cutoff** (معمولاً ۷ روز قبل از شروع دوره) امکان‌پذیر است.
5. پس از ثبت‌نام موفق، وضعیت Enrollment به `Active` تغییر می‌کند.

## ۲. قوانین پیش‌نیازها (Prerequisites)
1. کاربر برای ثبت‌نام در یک دوره، باید تمام **دوره‌های پیش‌نیاز** آن را با موفقیت گذرانده باشد (نمره قبولی در آزمون نهایی دوره پیش‌نیاز).
2. پیش‌نیازها به صورت زنجیره‌ای (درخت) پشتیبانی می‌شوند.
3. اگر دوره پیش‌نیاز نداشته باشد، شرط به طور خودکار برقرار است.

## ۳. قوانین پرداخت و وضعیت مالی
1. ثبت‌نام کاملاً منوط به **تایید پرداخت** است. پرداخت ناموفق → ثبت‌نام لغو می‌شود.
2. کاربران می‌توانند چندین دوره را در یک تراکنش پرداخت کنند (سبد خرید).
3. سیستم باید وضعیت مالی کاربر (Outstanding Balance، Refundable Amount) را مدیریت کند.
4. در صورت انصراف کاربر تا ۴۸ ساعت قبل از شروع دوره، امکان **بازگشت وجه** (Refund) وجود دارد (با کسر کارمزد).
5. پرداخت‌های ناموفق حداکثر ۳ بار retry خودکار دارند.

## ۴. قوانین مدیریت کلاس‌ها و زمان‌بندی
1. هر کلاس (Class/Session) حداکثر **ظرفیت تعریف‌شده** توسط ادمین را دارد.
2. اساتید (Instructor) فقط می‌توانند در زمان‌بندی‌هایی تدریس کنند که با تقویم آن‌ها تداخل نداشته باشد.
3. حداقل تعداد ثبت‌نام برای برگزاری کلاس: `min_students` (در غیر اینصورت کلاس لغو و وجوه بازگردانده می‌شود).

## ۵. قوانین آزمون و ارزیابی
1. کاربر فقط در صورتی می‌تواند در آزمون دوره شرکت کند که **ثبت‌نام فعال** داشته باشد.
2. هر دوره می‌تواند چندین آزمون (Midterm, Final, Quiz) داشته باشد.
3. نمره قبولی پیش‌فرض ۵۰٪ است (قابل تنظیم توسط ادمین).
4. کاربر می‌تواند آزمون را حداکثر **۲ بار** امتحان کند (قابل تنظیم).
5. نتایج آزمون بلافاصله پس از اتمام محاسبه و در پروفایل کاربر ذخیره می‌شود.

## ۶. قوانین محتوا و دسترسی
1. محتوای آنلاین فقط برای کاربران با ثبت‌نام فعال در دوره قابل دسترسی است.
2. محتوای آفلاین (فایل‌ها) پس از پرداخت موفق قابل دانلود است.
3. پیشرفت کاربر (Progress) در هر دوره به صورت درصد محاسبه می‌شود.

## ۷. قوانین عمومی و امنیتی
1. هر کاربر فقط یک حساب با ایمیل/شماره تلفن معتبر دارد (Unique).
2. ادمین‌ها و اساتید دسترسی جداگانه دارند (Role-Based Access Control).
3. تمام تغییرات مالی و ثبت‌نام‌ها باید **Audit Log** شوند.
4. سیستم باید قوانین سنی و محدودیت‌های قانونی (مثلاً دوره‌های خاص بزرگسالان) را رعایت کند.

## ۸. قوانین استثنا و Workflow
- **لغو دوره توسط سیستم**: اگر ظرفیت به حداقل نرسد، تمام ثبت‌نام‌ها لغو و وجوه بازگردانده شود.
- **تغییر زمان‌بندی**: اطلاع‌رسانی به تمام ثبت‌نام‌شدگان از طریق ایمیل/SMS الزامی است.

---

**نکته پیاده‌سازی**: این قوانین باید به صورت **Business Rules Engine** (مثل Drools یا در لایه Application Service) پیاده‌سازی شوند تا امکان تغییر آسان توسط Business Analyst وجود داشته باشد.

این مجموعه قوانین کامل، یکپارچه و همخوان با artefactهای قبلی (Class Diagram، ERD و Solution Design) است.
